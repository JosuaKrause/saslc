foldl' f z xs
    = if xs==[]
      then z
      else z !> foldl' f (f z @ hd xs) @ tl xs
    ;

enumFromTo x y = if x <= y then x : enumFromTo (x + 1) y else [];

sum = foldl' {n x -> n + x} 0;

main = sum @ enumFromTo 1 100000;

